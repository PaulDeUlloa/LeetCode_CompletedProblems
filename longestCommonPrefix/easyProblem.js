/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function (strs) {
  //we need to iterate through the string length
  //if there is no common prefix, return an empty string ""
  // we need to return
};
